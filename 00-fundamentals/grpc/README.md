# gRPC (sync, not REST)

Contract-first RPC over HTTP/2 + Protobuf — separate from REST clients.

| Item | Purpose |
|---|---|
| **`GRPC-OVERVIEW-AND-FLOW.md`** | gRPC vs REST + unary call flow + interview Q&A |
| **`grpc-demo/`** | Runnable server (:9090) + client (:8302) |

Theory also linked from `../01-SERVICE-COMMUNICATION.md`.  
REST clients (RestTemplate / RestClient / Feign): `../rest-clients-demo/`.
