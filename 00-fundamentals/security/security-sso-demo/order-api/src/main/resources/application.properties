server.port=8401
spring.application.name=order-api

# Validate JWTs issued by Keycloak (JWKS fetched from issuer)
spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8180/realms/interview

management.endpoints.web.exposure.include=health,info
