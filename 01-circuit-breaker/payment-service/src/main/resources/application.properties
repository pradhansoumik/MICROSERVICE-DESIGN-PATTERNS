server.port=8081
spring.application.name=payment-service

# Simulates real payment gateway latency / failure for Circuit Breaker demos
# failure.mode = NONE | SLOW | FAIL | ALTERNATE
payment.failure.mode=NONE
payment.slow.delay-ms=3000
